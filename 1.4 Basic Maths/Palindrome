n = int(input())
rev_num =0
dup = n 
while(n>0):
    last_digit = n%10
    rev_num = rev_num*10+last_digit
    n=n//10
if dup ==rev_num :
    print("Yes its Palindrome")
else:
    print("Not a Plaindrome")
